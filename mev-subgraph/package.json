{
  "name": "mev-subgraph",
  "version": "1.0.0",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ mev-detector-subgraph",
    "create-local": "graph create --node http://localhost:8020/ mev-detector-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ mev-detector-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mev-detector-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}